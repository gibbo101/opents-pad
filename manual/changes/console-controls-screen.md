---
title: Add a Controls screen drawing the controller under the controller scheme
category: feature
release: 0.2.0
targets:
- type: key
  id: ControlScheme
  effect: changed
credit: [gibbo101]
---

Under the `Controller` control scheme the options screen and the in-game menu offer a Controls screen that draws the controller with callouts naming what each button does, paged across the face buttons, the shoulders and triggers, and the sticks. The `KeyboardMouse` dialogs are unchanged.
